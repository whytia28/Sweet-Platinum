<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sweetPlatinum">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="com.example.sweetPlatinum.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <receiver
            android:name="com.example.sweetPlatinum.setting.AlarmReceiver"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>

        </receiver>

        <activity android:name="com.example.sweetPlatinum.register.RegisterActivity" />
        <activity android:name="com.example.sweetPlatinum.login.LoginActivity" />
        <activity android:name="com.example.sweetPlatinum.setting.SettingActivity" />
        <activity android:name="com.example.sweetPlatinum.battleActivity.MultiPlayerActivity" />
        <activity android:name="com.example.sweetPlatinum.menuActivity.MenuActivity" />
        <activity android:name="com.example.sweetPlatinum.landingPage.LandingActivity" />
        <activity android:name="com.example.sweetPlatinum.splashScreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>